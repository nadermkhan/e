extern func printf(format: String, ...) -> Int

func main() -> Int {
    var greeting = "Hello, World from Elegant Compiler!"

    printf(format: "%s | Number: %d\n", greeting, 2026)
    
    return 0
}
