struct Point {
    var x: Int
    var y: Int

    func move(amount: Int) {
        self.x = self.x + amount
        self.y = self.y + amount
    }
}

func main() -> Int {
    var p = Point()

    p.x = 10
    p.y = 5

    p.move(amount: 20)

    return p.x + p.y
}
